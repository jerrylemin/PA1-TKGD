# PA4 remediation round 2 decision log

Date: 2026-08-23  
Repository: `C:\Users\Administrator\Documents\MEGA\tkgd\PA`  
Starting local handoff: `READY_FOR_REAL_PARTICIPANTS` in prior memory; source audit shows `NOT_READY` for this round until local defects are fixed.  
Evidence boundary: no real participant rows, recordings, timings, questionnaire answers, interview records, findings, or external URLs were created.

## Reproduction evidence

- `python PA4/qa/validate_chess_scenario.py` → PASS, but only for the existing geometry/path checks.
- `python -m unittest discover -s PA4/study/analysis -p 'test_analyze_study.py'` → 12 tests PASS; no audio-only regression test exists.
- Browser reproduction at `http://127.0.0.1:4173/index.html`: intro exposed `Qa4`, `c3`, and one highlighted square; mistake exposed `Qc2` before reveal; trial contained 64 square buttons and two direct move buttons; clicking `c2` alone succeeded; practice had no square input and `Complete practice` alone produced `Idea saved.`; FIFA study had 8 researcher-chrome DOM nodes, 0 visible nodes, and one reset plus one unavailable-preview control in the DOM.
- Recording reproduction with temporary files: missing → `MISSING_RECORDING`; text `.mp4` → `INVALID_RECORDING`; audio-only MP4 → `VERIFIED_RECORDING` (defect); valid video MP4 → `VERIFIED_RECORDING`.
- Current PDF extraction: Hi-fi PDF contained `?mode=study&product;=fifa` and `?mode=study&product;=chess`; Summative PDF page line counts were `[8, 54, 33, 59, 3, 50]`, proving an avoidable near-empty page 5. Summative procedure text contained fixed FIFA-first wording and hardcoded zero-session language.
- `PA4/final/Group10-PA4.zip` already existed before this round and the package source had no readiness gate; it was preserved and not deleted during reproduction.

## Issue decisions

| issue_id | memory_claim | source_evidence | reproduced | root_cause | affected_files | smallest_fix | validation | decision |
|---|---|---|---|---|---|---|---|---|
| R2-01 | Study flow should not reveal the measured answer before participant action. | `prototype/app.js` intro renders `phase: "mistake"`; mistake state renders the better-move chip before reveal. | YES | Later-state board and answer copy are reused in earlier states. | `prototype/app.js`, browser QA, screenshots/reports | Make intro context-only; make mistake state consequence-only; reveal answer only in better/trial stage. | Browser DOM/text checks for intro, mistake, trial. | PATCH |
| R2-02 | Chess validation should establish pedagogical ground truth, not only legality/geometry. | `qa/validate_chess_scenario.py` checks piece shapes, path clearance, and an attack distinction; it does not apply the mistake/better moves or verify the consequence. | YES | Validator proves a board shape but not the claimed learning outcome. | `prototype/app.js`, `qa/validate_chess_scenario.py`, validation record | Keep one authoritative scenario with explicit move application, observable material-safety consequence, rationale, and practice concept; validate every claim deterministically. | Scenario validator plus source/board assertions. | PATCH |
| R2-03 | Trial must measure chess move execution. | Trial handler compares only `target.dataset.square`; direct `Play Qc2`/`Play Qa4` buttons bypass board input. | YES | Destination-only selection is mistaken for a move. | `prototype/app.js`, `styles.css`, browser QA | Add `selectedSquare`; require source-piece click then destination click; remove direct move shortcuts from participant flow. | Source→destination, destination-first, wrong move, retry, correct move tests. | PATCH |
| R2-04 | Practice must require bounded interactive input. | Practice reuses the review board and completes from a standalone button. | YES | Practice is a passive acknowledgement state. | `prototype/app.js`, `styles.css`, browser QA | Add a related deterministic practice position and source→destination interaction with wrong/retry/correct feedback. | Practice input/retry/success checks. | PATCH |
| R2-05 | Participant study mode must not expose researcher/demo controls. | CSS hides `.study-researcher-chrome`, but controls remain in DOM; `fifa-preview-error` and `fifa-reset` lack study guards. | YES | Visual hiding is used instead of participant-mode DOM/state separation. | `prototype/index.html`, `prototype/app.js`, `styles.css`, browser QA | Render/remove researcher-only nodes for study mode and guard researcher actions. | Visible, DOM, focus, and action checks in study mode; presenter regression. | PATCH |
| R2-06 | `VERIFIED_RECORDING` requires a real video stream. | `verify_recording()` asks ffprobe only for format duration. | YES | Positive-duration audio is treated as video. | `study/analysis/analyze_study.py`, tests, recording policy | Probe `stream=codec_type`; require at least one `video` stream and positive duration. | Missing/text/audio-only/valid-video tests; no-ffprobe unverified test. | PATCH |
| R2-07 | Report procedure must follow stored `condition_order`. | Study plan defines both orders; report source says FIFA tasks then reset then Chess. | YES | Report prose is a fixed sequence independent of participant assignment. | `study/study-plan.md`, `study/facilitator-script.md`, report builder | Use order-neutral wording and explicitly state both condition sequences. | DOCX/PDF extraction and source assertions. | PATCH |
| R2-08 | `success_score` must be operationally defined. | Protocol/report score 1 includes undefined “meaningful hesitation”; analysis accepts only numeric 0/1/2. | YES | Hesitation is an unbounded judgment bundled into score. | Study protocol, analysis/report builder, tests | Remove hesitation from score; define it separately as an observable pause threshold recorded in notes/timestamp. | Protocol and report text checks; score semantics test. | PATCH |
| R2-09 | Study URLs must survive PDF rendering exactly. | `pdf_p()` passes raw text to ReportLab; extracted output contains `product;=`. | YES | XML entity parsing corrupts raw ampersands in paragraph text. | `scripts/build_pa4_reports.py`, PDF QA | Escape paragraph text once at the renderer boundary while preserving literal URL output. | Build/extract exact FIFA/Chess URLs and reject `product;=`. | PATCH |
| R2-10 | DOCX/PDF should share substantive report content. | `build_*_docx()` and `build_*_pdf()` contain separate duplicated prose branches. | YES | No canonical report content model exists. | `scripts/build_pa4_reports.py`, generated reports | Centralize report model data and have DOCX/PDF renderers consume it; retain formatting-specific branches only. | Compare normalized substantive text/section values from DOCX and PDF. | PATCH |
| R2-11 | Summative report status must consume canonical analysis state. | Builder contains hardcoded `0 verified`, header-only, and fixed gate language and never reads `analysis-result.json`. | YES | Reports are authored from pre-study literals instead of analyzer output. | `study/analysis/analyze_study.py`, report builder/tests/reports | Load canonical analysis JSON into report model; render counts/gates dynamically and test isolated synthetic fixture output without touching real evidence. | Empty real state, synthetic complete fixture, return-to-real-state checks. | PATCH |
| R2-12 | Official package must not be generated before submission evidence is ready. | `package_pa4.py` always validates filenames then writes `Group10-PA4.zip`; existing official archive confirms the gate is absent. | YES | Working-evidence and official package paths are separated but official readiness is not enforced. | `scripts/package_pa4.py`, package QA | Add explicit readiness gate with blocker list before official ZIP write; keep working ZIP available. | Official command → `REFUSED`; working command → PASS; no deletion of pre-existing archive. | PATCH |
| R2-13 | Weekly Report/template blockers affect submission, not participant readiness. | Readiness exists only in documents; no executable state model distinguishes local participant readiness from external submission blockers. | YES | A single implicit package/document state is used for separate milestones. | `scripts/package_pa4.py`, QA/memory state docs | Define local readiness from local QA gates and submission readiness from external/template/evidence gates; report both explicitly. | Matrix and package gate checks. | PATCH |
| R2-14 | Summative PDF should avoid accidental near-empty pagination. | Current PDF page 5 has only 3 non-empty lines; source inserts `PageBreak()` after a paragraph that already spills. | YES | Forced page break follows an overflowed paragraph. | `scripts/build_pa4_reports.py`, PDF QA | Remove the forced break at that boundary and let the final section flow naturally. | Rebuild and assert no near-empty page; inspect page text/render. | PATCH |

## Phase 1 decision table outcome

All fourteen round-2 defects are locally reproducible or source-proven and require the scoped patches above. No external blocker authorizes fabricating evidence. The target after implementation is `READY_FOR_REAL_PARTICIPANTS`; `SUBMISSION_READY` remains out of scope and must not be asserted.
